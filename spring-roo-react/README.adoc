= Spring Roo and React JS

This proof of concept contains an Spring Boot application generated using the Spring Roo Shell that serves a React.js application to the front-end and consumes the JSON services from the Rest API of the Spring Boot application.
