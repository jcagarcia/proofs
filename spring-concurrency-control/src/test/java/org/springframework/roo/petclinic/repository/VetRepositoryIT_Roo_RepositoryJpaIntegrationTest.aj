// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.springframework.roo.petclinic.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.roo.petclinic.config.SpringDataJpaDetachableRepositoryConfiguration;
import org.springframework.roo.petclinic.dod.DataOnDemandConfiguration;
import org.springframework.roo.petclinic.domain.Vet;
import org.springframework.roo.petclinic.domain.VetInfo;
import org.springframework.roo.petclinic.domain.dod.VetDataOnDemand;
import org.springframework.roo.petclinic.repository.VetRepository;
import org.springframework.roo.petclinic.repository.VetRepositoryIT;
import org.springframework.test.context.junit4.SpringRunner;

privileged aspect VetRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: VetRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: VetRepositoryIT: @DataJpaTest;
    
    declare @type: VetRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException VetRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private VetRepository VetRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private VetDataOnDemand VetRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return VetRepository
     */
    public VetRepository VetRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return VetDataOnDemand
     */
    public VetDataOnDemand VetRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void VetRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomVet())
            .as("Check data on demand for 'Vet' initializes correctly by getting a random Vet")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'Vet' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.findOneShouldReturnExistingVet() {
        // Setup
        Long id = getRandomVetId();
        
        // Exercise
        Vet vet = getRepository().findOne(id);
        
        // Verify
        assertThat(vet).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'Vet' is the same used to look for it")
            .isEqualTo(vet.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.findAllShouldReturnAllVets() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<Vet> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.persistShouldGenerateIdValue() {
        // Setup
        // Exercise
        Vet vet = getDod().getNewRandomTransientVet();
        
        // Verify
        assertThat(vet).as("Check the Data on demand generated a new non null 'Vet'").isNotNull();
        assertThat(vet.getId()).as("Check the Data on demand generated a new 'Vet' whose id is null")
            .isNull();
        try {
            vet = getRepository().saveAndFlush(vet);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter
                .hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".")
                    .append(cv.getPropertyPath()).append(": ").append(cv.getMessage())
                    .append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        assertThat(vet.getId()).as("Check a 'Vet' (%s) id is not null after been persisted", vet)
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.deleteShouldMakeVetUnavailable() {
        // Setup
        Long id = getRandomVetId();
        Vet vet = getRepository().findOne(id);
        
        // Exercise
        getRepository().delete(vet);
        
        // Verify
        assertThat(getRepository().findOne(id))
        .as("Check the deleted 'Vet' %s is no longer available with 'findOne'", vet).isNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllVets() {
        // Exercise
        Page<VetInfo> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void VetRepositoryIT.findAllCustomNotFilteredPagedShouldReturnAVetsPage() {
        // Exercise
        Page<VetInfo> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long VetRepositoryIT.getRandomVetId() {
        Vet vet = getDod().getRandomVet();
        Long id = vet.getId();
        assertThat(id).as("Check the Data on demand generated a 'Vet' with an identifier").isNotNull();
        return id;
    }
    
}
