type Travel {
    id: ID!
    name: String!
    country: String
}

type Query {
    findAllTravels: [Travel]
}

type Mutation {
    create(name: String!, country: String!) : Travel!
}

schema {
    query: Query
    mutation: Mutation
}